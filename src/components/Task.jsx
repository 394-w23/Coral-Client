const Task = (taskID) => {
    // const [count, setCount] = useState(0);
    id = 1;
    return (
        <div className="eventClass">
            <h1>Title</h1>
        </div>
    );
};

export default Task;